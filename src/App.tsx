// TypeScript React component that calculates and displays the age based on birth year and current year
import { AppComponent } from './AppComponents';

export default function App() {
  return <AppComponent />;
}